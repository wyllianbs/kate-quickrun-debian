# Screenshots

Place the README screenshots here with these exact names:

- `menu.png`        — the Quick Run toolbar button with the destination menu open.
- `config.png`      — Settings → Configure Kate → Quick Run (the config page).
- `own-terminal.png`— the Quick Run Terminal docked beside the editor, running
                      an interactive program (e.g. test/main.c waiting for input).

Suggested: capture on a dark theme, PNG, cropped to the relevant area.
